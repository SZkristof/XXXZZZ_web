---
import SectionHeading from '@/components/SectionHeading.astro';
import { faqs } from '@/data/proof';

interface Props { limit?: number }
const { limit } = Astro.props;
const items = limit ? faqs.slice(0, limit) : faqs;
---

<!-- Native <details>: fully accessible and keyboard-operable with zero JS. -->
<section class="section bg-white" id="gyik">
  <div class="container-x">
    <div class="grid gap-12 lg:grid-cols-[0.8fr_1.2fr] lg:gap-20">
      <div class="lg:sticky lg:top-28 lg:self-start">
        <SectionHeading
          eyebrow="Gyakori kérdések"
          title="Amit a legtöbben megkérdeznek"
          lead="Ha nincs köztük a tiéd, írj vagy hívj — 30 percben úgyis mindent átbeszélünk."
        />
      </div>

      <div class="divide-y divide-ink-200 border-y border-ink-200">
        {items.map((f) => (
          <details class="group">
            <summary
              class="flex cursor-pointer list-none items-start justify-between gap-6 py-6
                     font-display text-lg font-bold text-ink-950 transition-colors
                     hover:text-ember-700 [&::-webkit-details-marker]:hidden"
            >
              {f.q}
              <span
                class="relative mt-1 grid h-6 w-6 shrink-0 place-items-center rounded-full
                       bg-ink-100 transition-colors group-open:bg-ember-500"
                aria-hidden="true"
              >
                <span class="absolute h-0.5 w-3 rounded bg-ink-700 transition-colors group-open:bg-white"></span>
                <span
                  class="absolute h-3 w-0.5 rounded bg-ink-700 transition-all duration-300
                         group-open:rotate-90 group-open:bg-white"
                ></span>
              </span>
            </summary>
            <div class="pb-6 pr-10">
              <p class="leading-relaxed text-ink-600">{f.a}</p>
            </div>
          </details>
        ))}
      </div>
    </div>
  </div>
</section>
